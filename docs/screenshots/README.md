Screenshots shown in the project README.

Name them so the alphabetical order is the order you want:

    01-downloads.jpg
    02-search.jpg
    03-sync.jpg
    04-settings.jpg

PNG works just as well — it is the better format for screenshots, since flat
interface colours and text survive it without compression artefacts. Whichever
you use, the extension has to match the `<img>` tags at the top of
`../../README.md`.
